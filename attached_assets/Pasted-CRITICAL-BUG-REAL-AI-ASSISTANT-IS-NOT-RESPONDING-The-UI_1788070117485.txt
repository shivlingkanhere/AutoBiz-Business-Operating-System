CRITICAL BUG: REAL AI ASSISTANT IS NOT RESPONDING

The UI/layout issue has been improved. Now focus specifically on the AI chat integration.

Current behavior:
- The AI chat page loads correctly.
- User messages appear correctly in the chat.
- Example: "hi"
- But instead of an AI response, the UI shows:
  "I couldn’t reach the AI assistant. Please try again."

This means the chat request is failing somewhere between the frontend, backend API, and AI provider.

DO NOT just change the error message.
DO NOT create a fake/hardcoded response.
DO NOT mock the AI.
Find and FIX the actual root cause.

==================================================
DEBUG THE COMPLETE REQUEST FLOW
==================================================

Trace this exact flow:

Chat UI
→ frontend chat request
→ backend chat endpoint
→ Clerk authentication
→ current user/business identification
→ conversation/history retrieval
→ FAQ/context retrieval
→ AI provider request
→ AI provider response
→ backend response
→ frontend response parsing
→ assistant message rendering

Find exactly where the request fails.

==================================================
CHECK FRONTEND
==================================================

Inspect the chat component and API client.

Verify:
- Correct API endpoint URL
- Correct HTTP method
- Correct request body
- Correct headers
- Authentication/session handling
- Clerk token/session handling
- Correct response parsing
- Correct error handling

Use the browser Network request to determine:
- Request URL
- HTTP status
- Request payload
- Response body

Do not assume the frontend is correct.

==================================================
CHECK BACKEND
==================================================

Inspect the actual chat API route/controller/service.

Verify:
- Route is registered
- Request reaches the backend
- Authentication works
- User ID is available
- Business/store ID is available
- Request body is validated correctly
- Conversation history query works
- FAQ query works
- AI service is actually called
- AI response is returned

Add safe diagnostic logging where necessary.

IMPORTANT:
Never log or expose API keys, Clerk secrets, tokens, passwords, or other secrets.

==================================================
CHECK AI PROVIDER
==================================================

Inspect the existing AI integration.

Verify:
- AI SDK/package is installed correctly
- AI provider is configured correctly
- Environment variable name matches the code
- API key is actually available to the BACKEND runtime
- Model name is valid
- API request format is correct
- The backend is not accidentally looking for the API key under a wrong environment variable
- No server/client environment-variable mistake exists
- No unsupported/invalid model configuration exists
- No timeout is causing the request to fail

The AI key is already securely configured in the Replit environment.

Do NOT expose the key.
Do NOT hardcode the key.
Do NOT commit the key to GitHub.
Do NOT move the secret into frontend/client code.

==================================================
VERY IMPORTANT — FAQ MUST NOT BLOCK AI
==================================================

The previous implementation behaved like an FAQ-only chatbot.

Fix this architecture.

FAQ should be contextual knowledge, NOT a prerequisite for generating an AI response.

For example:

User:
"hi"

The AI must respond naturally.

User:
"How can I increase my shop sales?"

The AI must generate a useful answer even if there is no matching FAQ.

User:
"Which products should I reorder?"

If inventory data exists, use the real inventory data.

User:
"What is our return policy?"

Use the relevant FAQ/business policy if available.

If FAQ retrieval returns zero results, the AI request MUST still proceed.

An empty FAQ result must NEVER cause the chat request to fail.

==================================================
CHECK ERROR HANDLING
==================================================

The current frontend displays:

"I couldn’t reach the AI assistant. Please try again."

Find what backend error causes this fallback.

Do not hide the original error during development.

Temporarily add safe server-side diagnostics so we can determine whether the failure is:

- 401/403 authentication
- 400 validation
- 404 endpoint
- 500 backend error
- AI API authentication failure
- invalid model
- missing environment variable
- database error
- timeout
- response parsing error
- CORS/network issue

Do not expose sensitive information to the user.

Return a structured error response from the backend.

==================================================
TEST WITH REAL REQUESTS
==================================================

After fixing the integration, actually run the application and test:

1. "hi"

Expected:
A real AI-generated conversational response.

2. "How can I increase my shop sales?"

Expected:
A useful AI-generated response.

3. "Which products should I reorder?"

Expected:
Use actual inventory/business data if available.

4. Ask a question that is NOT in FAQ.

Expected:
AI still responds normally.

5. Ask a follow-up question.

Expected:
AI understands the previous conversation.

==================================================
DO NOT BREAK EXISTING FEATURES
==================================================

Keep:
- Clerk authentication
- Existing database
- Existing API contract where possible
- Existing business/store context
- Existing FAQ system
- Existing chat UI
- Existing inventory/sales/customer features

Do not rebuild the application.

Do not replace the current AI integration with mock data.

Do not create hardcoded chatbot responses.

==================================================
FINAL VERIFICATION
==================================================

Do not stop after making code changes.

Actually:

1. Inspect
2. Diagnose
3. Implement
4. Run backend
5. Run frontend
6. Test real chat request
7. Inspect Network response
8. Inspect server logs
9. Fix any remaining error
10. Retest

The task is NOT complete until a real message such as "hi" produces an actual AI-generated response in the AutoBiz chat UI.

Also verify that FAQ answers and normal AI answers both work.